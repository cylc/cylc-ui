The log view now supports two viewing modes toggled via a HEAD/TAIL button: HEAD mode (default) shows the start of the file and follows new lines, while TAIL mode shows the most recent lines. Users can configure a global "Maximum log lines" setting in their User Profile to control memory usage and UI performance.
